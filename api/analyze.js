export default async function handler(req, res) { try { const {url,caption} = req.body; const r = await fetch('https://api.anthropic.com/v1/messages', {method:'POST',headers:{'Content-Type':'application/json','x-api-key':process.env.VITE_ANTHROPIC_API_KEY,'anthropic-version':'2023-06-01'},body:JSON.stringify({model:'claude-sonnet-4-5',max_tokens:2000,system:'Respond with valid JSON only.',messages:[{role:'user',content:'Create a workout plan JSON with title, tag, duration, level, influencer, source, notes, exerciseList (6 exercises with name/sets/reps/rest/weight/notes). Based on: URL=' + (url||'') + ' Caption=' + (caption||'')}]})}); const d = await r.json(); const t = d.content[0].text.replace(/```json|```/g,'').trim(); const p = JSON.parse(t); if(url&&url.includes('youtube.com/watch')){try{p.videoId=new URL(url).searchParams.get('v')}catch{}} res.status(200).json(p); } catch(e) { res.status(500).json({error:e.message}); } }
